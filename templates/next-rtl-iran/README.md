# Next.js RTL + Iran forms kit

قالب کامل با:

- `react-hook-form` + `mahsaagent/zod`
- `react-multi-date-picker` (جلالی)
- `useBankSync` برای شبا
- آدرس آبشاری از `officialAddressCascade`

```bash
cd templates/next-rtl-iran
npm install
npm run dev
```
